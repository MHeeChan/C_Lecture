가져오는 자료형에 상관없이 포인터 변수의 크기는 같다.
증감단위는 가져오는 자료형의 영향
int면) pInt+1 => sizeof(int) 단위로 증가


32bit // 4바이트
64bit // 8바이트

배열의 이름 -> 그 배열의 시작 주소

int iArr[10] = {};
*(iArr + 1) = 10; // iArr[1] = 10;

// 배열의 특징
메모리가 연속적인 구조이다
배열의 이름은 배열의 시작 주소이다.
